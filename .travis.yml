language: generic
sudo: required

services:
    - docker
before_install:
    - docker build -t ashview/udemymulti -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true ashview/react-test npm test
  
after-success:
    - docker build -t ashview/udemymulti-client ./client
    - docker build -t ashview/udemymulti-nginx ./nginx
    - docker build -t ashview/udemymulti-server ./server
    - docker build -t ashview/udemymulti-worker ./worker
    # login to docker cli
    - echo "$DOCKER_PASSWORD" │ docker login -u "$DOCKER_ID" --password-stdin
    - docker push ashview/udemymulti-client
    - docker push ashview/udemymulti-nginx
    - docker push ashview/udemymulti-server
    - docker push  ashview/udemymulti-worker
    # take out an push  >?<>>?:{@}~~}¬!""¬``¬





